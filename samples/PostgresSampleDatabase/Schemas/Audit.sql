-- Schemas are declared, first-class objects in Squill: they are created explicitly,
-- never conjured implicitly when a table references them. Declaring the schema here lets
-- the tables below live in it.
CREATE SCHEMA audit;
